flask>=3.0.0
python-docx>=1.1.0
odfpy>=1.4.1
rapidfuzz>=3.6.0
faster-whisper>=1.0.3
pyannote.audio>=3.1.0
soundfile>=0.12.0
# transformers and accelerate are no longer required for transcription
# (faster-whisper uses CTranslate2 directly); they are kept here only as
# transitive deps for pyannote/speechbrain — remove if not needed.
# openai-whisper is no longer used (replaced by faster-whisper)
Pillow>=10.0.0
cryptography>=42.0.0
argon2-cffi>=23.0.0
# OCR: docTR (Win/Linux), Apple Vision (macOS — installeras via pyobjc)
python-doctr[torch]>=0.8.0 ; sys_platform != "darwin"
pyobjc-framework-Vision>=9.0 ; sys_platform == "darwin"
